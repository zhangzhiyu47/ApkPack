# JNI bootstrap loading removed.
# Bootstrap is now handled by BootstrapInstaller.java which extracts
# bootstrap.tar.xz from assets at runtime.
# This file is kept as a placeholder for NDK build compatibility.
LOCAL_PATH := $(call my-dir)
